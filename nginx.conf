server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  location / {
    try_files $uri /index.html;
  }

  # Hardening defaults (gzip, security headers, and long-cache strategy for
  # immutable assets) are intentionally out of scope for this iteration.
}
